# Cost optimization

For cost-reduction requests on cloud compute:

1. Rank idle or oversized instances first.
2. Prefer reversible actions (stop, resize) over delete.
3. Keep evidence from metrics in the recommendation.
4. Never auto-execute; wait for human approve/edit/reject.
